apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service-deployment
  labels:
    app: delivery-service
spec:
  replicas: 1
  template:
    metadata:
      name: delivery-service
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: ewangclarks/node-back-end:ptds
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          env:
            - name: jwt_private_key
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: jwtPrivateKey
            - name: loggly_token
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: logglyToken
            - name: amqp_password
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: amqpPassword
            - name: db_connection_url
              valueFrom:
                configMapKeyRef:
                  name: app-config-map
                  key: dbConnectionUrl
            - name: NODE_ENV
              valueFrom:
                configMapKeyRef:
                  name: app-config-map
                  key: nodeEnv
      imagePullSecrets:
        - name: registry-credentials
      restartPolicy: Always
  selector:
    matchLabels:
      app: delivery-service
